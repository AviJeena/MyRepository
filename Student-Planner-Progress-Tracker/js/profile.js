document.addEventListener('DOMContentLoaded', () => {
    console.log("Profile engine successfully initialized.");
    // This can be easily expanded later to fetch API metrics directly from a database route:
    // fetch('/api/user/profile-metrics')
    //   .then(res => res.json())
    //   .then(data => updateUI(data));
});